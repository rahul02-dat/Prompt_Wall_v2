# Research track dependencies — separate from the gateway's requirements.txt
# since torch/transformers are heavy and only needed for oracle generation.
torch==2.5.1
transformers==4.46.0
accelerate==1.1.0
scikit-learn==1.5.2
joblib==1.4.2
numpy==2.1.3
